[project]
name = "lume-market-maker"
version = "0.1.0"
description = "Python SDK for Lume prediction markets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "web3>=7.0.0",
    "eth-account>=0.13.0",
    "requests>=2.32.0",
    "websockets>=12.0",
]
